language: python
python:
  - '3'

env:
  - DB_NAME=travis_ci_test DB_USER=postgres DB_HOST=localhost DB_PORT='5432' DB_PASSWORD=''

install:
  - pip install -r requirements.txt
script:
  - python manage.py test
services:
  - postgresql
before_script:
  - psql -c 'create database travis_ci_test;' -U postgres
